# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
aiosqlite==0.20.0
greenlet>=3.0

# Validation
pydantic==2.10.3
pydantic-settings==2.6.1

# AI / LLM
anthropic>=0.84.0
httpx==0.28.1

# Audio (Phase 2+)
# PyTorch must be installed separately with the CUDA wheel — see install.bat
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
numpy>=2.0
sounddevice>=0.5.0
faster-whisper==1.1.0
silero-vad>=6.0

# TTS (Phase 3+)
coqui-tts>=0.27.0
transformers>=4.57,<5.0

# TTS voices (Phase 8+) — Edge-TTS for accent variety; imageio-ffmpeg bundles
# a self-contained ffmpeg binary for MP3→WAV decode (no system ffmpeg needed)
edge-tts>=6.1
imageio-ffmpeg>=0.4

# Embeddings (Phase 5+)
sentence-transformers>=3.0
sqlite-vec==0.1.6

# Observability (Phase 8+) — OTel client for Arize Phoenix LLM tracing
# Phoenix server runs separately: pip install arize-phoenix && phoenix serve
arize-phoenix-otel>=0.1
openinference-instrumentation-anthropic>=0.1
opentelemetry-sdk>=1.20

# Utilities
python-dotenv==1.0.1
structlog==24.4.0
PyYAML>=6.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
